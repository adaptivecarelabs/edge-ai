# Raspberry Pi edge-server image

Provisioning target: Raspberry Pi 4 (4GB RAM, 32GB SD), Ubuntu 22.04 LTS, Docker.

Placeholder for Phase 1 task "RPi edge-server image" (`docs/project_plan.md`). Will contain the
provisioning script/image build (Ansible or shell) that installs Docker and brings up the same
`docker-compose.yml` stack as local dev, sized for the Pi's memory/thermal constraints.

Not yet implemented.
