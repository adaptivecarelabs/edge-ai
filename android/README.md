# android

React Native CHW app: offline-first (PouchDB), consent workflow, camera capture + OCR (PP-OCRv5) +
medical NER review, patient history timeline, offline rule-based + INT8 quantized LLM triage engine,
ASR.

Not yet scaffolded. See Phase 2 "Android" tasks in `../docs/project_plan.md`.
